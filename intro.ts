var user = { name:"gunit", age:10 }
console.log("gunit")
console.log(user.name)


// the command tsc intro.ts will create a file in js which automaticlly convert the ts in js 

// in ts we can never use a function without calling with proper type

let greetings: string = "hello gunit"

let myNum = 6

greetings.toLowerCase()

console.log(greetings);


export {}